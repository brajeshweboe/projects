<?xml version="1.0" encoding="UTF-8"?>
<site-conf xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://ofbiz.apache.org/Site-Conf" xsi:schemaLocation="http://ofbiz.apache.org/Site-Conf http://ofbiz.apache.org/dtds/site-conf.xsd">
    <!-- The controller elements that are common to all OFBiz components
         can be found in the following xml file. A component can override the
         elements found in the common-controller.xml file. -->
    <description>Gaadizo Component Site Configuration File</description>

    <!-- Events to run on every request before security (chains exempt) -->
    <!--    
    <preprocessor>
    </preprocessor>
    -->
    <!-- Events to run on every request after all other processing (chains exempt) -->
    <!--
    <postprocessor>
        <event name="test" type="java" path="org.apache.ofbiz.webapp.event.TestEvent" invoke="test"/>
    </postprocessor>
    -->

    <!-- Request Mappings -->

      <request-map uri="invoicelist">
        <security https="true" auth="true"/>
        <response name="success" type="view" value="InvoiceList"/>
        <response name="error" type="view" value="InvoiceList"/>
    </request-map>
     <request-map uri="InvoicePDF">
        <security https="true" auth="true"/>
        <response name="success" type="view" value="OrderPDF"/>
        <response name="error" type="view" value="OrderPDF"/>
    </request-map>
    <!-- View Mappings -->
    <view-map name="InvoiceList" type="screen" page="component://gaadizo/widget/InvoiceScreens.xml#InvoiceList"/>
        <view-map name="OrderPDF" type="screenfop" page="component://gaadizo/widget/GaadizoPrintScreens.xml#OrderPDF" content-type="application/pdf" encoding="none"/>
    
   

</site-conf>
